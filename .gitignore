.env
__pycache__/
*.pyc
*.backup
venv/

# состояние песочницы — генерится setup_sandbox.sh/start_sandbox.sh
# на конкретном сервере, не source
sandbox/queue_num
sandbox/nfqws2_sandbox.conf
sandbox/nfqws2_sandbox.pid
sandbox/nfqws2_sandbox.debug.log

# TLS-сертификат/ключ панели (Cloudflare Origin CA) -- живут тут на
# конкретном сервере, никогда не коммитятся, см. README "Публикация
# панели через Cloudflare"
panel/tls/

# Переопределение PROFILE_TARGETS для конкретного сервера/провайдера
# (см. orchestrator/auto_promoter.py::_load_profile_targets) -- сверено
# под структуру ОДНОГО конкретного /opt/zapret2/config, не source.
orchestrator/profile_targets.*.json
